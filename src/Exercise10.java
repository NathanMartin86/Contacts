/**
 * Created by macbookair on 11/10/15.
 */
public class Exercise10 {
    public static void main(String[] args) {
        int num = 12345;
        String numStr = String.valueOf(num);
        String numStrReversed = "";

        /*
        for (int i = numStr.length()-1; i>=0; i--) {
            numStrReversed += numStr.charAt(i);
        }
        int numReversed = Integer.valueOf(numStrReversed);
        System.out.println(numReversed);
        }
        */

    }

}
       /*
        int remainder = 0;
        int reverse = 0;

        do {
          remainder = num % 10;
            reverse = reverse *10 +remainder;        // This is the way I solved it! Yay!! :)
            num = num/10;
        }
        while (num > 0);

        System.out.println(reverse);

        // int numReversed = 54321;

    }
}
*/